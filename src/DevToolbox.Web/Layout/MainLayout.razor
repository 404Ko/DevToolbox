@inherits LayoutComponentBase

<div class="app-container">
    @* 左侧导航 - 固定不可收起 (AI Accept [Modified]) *@
    <aside class="sidebar">
        <div class="sidebar-header">
            <span class="logo">🔧 DevToolbox</span>
        </div>

        <div class="sidebar-search">
            <input type="text"
                   placeholder="🔍 搜索工具..."
                   @bind="SearchKeyword"
                   @bind:event="oninput" />
        </div>

        <nav class="sidebar-nav">
            <NavMenu SearchKeyword="@SearchKeyword" />
        </nav>
    </aside>

    @* AI Accept [Modified]: 简化结构，用单个div替代main/article嵌套 *@
    <div class="main-area">
        <div class="content">
            @Body
        </div>
        <footer class="app-footer">
            © @DateTime.Now.Year DevToolbox · All rights reserved · Owner: pengjie
        </footer>
    </div>
</div>

@code {
    // AI Accept [Modified]: Removed sidebar collapse, kept theme toggle
    private bool IsDarkTheme { get; set; } = false;
    private string SearchKeyword { get; set; } = string.Empty;

    private void ToggleTheme()
    {
        IsDarkTheme = !IsDarkTheme;
    }
}
